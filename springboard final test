def max_array_diff(inp):
    # complete code here

    (inp) = [int(s) for s in inp.split(' ')]

    diff_list = []
    for i in range(2, len(x)-1):    
        diff_list.append(x[i] - x[j])
        sorted(diff_list)
    return diff_list[0]

# Accept input from standard input
phrase = input()
print(max_array_diff(phrase))
